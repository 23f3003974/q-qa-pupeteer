name: Playwright QA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check-sum:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      # This is the critical step the grader identifies by name
      - name: 23f3003974@ds.study.iitm.ac.in
        run: python scrape_sum.py
